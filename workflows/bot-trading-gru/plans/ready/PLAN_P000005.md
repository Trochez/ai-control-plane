---
schema: agent-handoff/v1
workflow_id: bot-trading-gru
plan_id: P000005
parent_run_id: R000004
status: READY

target_repository: bot_trading
target_branch: feature/GRU
expected_base_sha: 1afb326bead32e2f928530e33850b5651e46b9b1

execution_mode: local_opencode
---

# Objective

Prove the remediated OpenCode control-plane lifecycle end-to-end in unattended `run` mode, without any manual recovery or target-repository mutation.

This is the final read-only smoke test for the chain:

`ChatGPT review/PLAN -> handoff-to-planner -> wait-next-plan -> detached spawn-opencode -> run-opencode-session -> evidence validation/retry if needed -> complete-run -> publish-run -> REVIEW_READY`.

The target repository `/mnt/d/works/bot_trad/bot_trading` is strictly read-only for this PLAN.

# Mandatory Preconditions

Before any action:

1. Read this complete PLAN and `WORKFLOW.md`.
2. Verify this is `P000005` with parent `R000004`.
3. Verify target repository path is `/mnt/d/works/bot_trad/bot_trading`.
4. Verify active target branch is `feature/GRU`.
5. Verify target HEAD is exactly `1afb326bead32e2f928530e33850b5651e46b9b1`.
6. Verify the target working tree is clean.
7. Confirm no target-repository mutation is required by this PLAN.

If a safety precondition fails, create truthful BLOCKED evidence and do not mutate the target repository.

# Authorized Work

This PLAN authorizes only read-only inspection of `bot_trading` plus creation of R000005 evidence artifacts in the control-plane run directory assigned by the orchestrator.

Do not change control-plane lifecycle state yourself. Do not call `complete-run` or `publish-run`; those belong to the orchestrator.

# Required Execution Evidence

Create the required evidence for R000005 in the assigned run directory:

- `RESULT.md`
- `evidence.json`
- `git-status-after.txt`
- `head-sha-after.txt`
- `branch-after.txt`

`RESULT.md` must state:

- PLAN and parent run verified;
- target repository, branch and HEAD verified;
- target tree was clean before and after;
- no target files were modified;
- this run was executed through the unattended control-plane path;
- whether this OpenCode attempt is the normal first attempt or a recovery attempt, if that can be determined from the assigned run artifacts;
- no lifecycle or Planner decision was performed by OpenCode.

`evidence.json` must include at minimum:

- `workflow_id`
- `plan_id`
- `run_id`
- `parent_run_id`
- `repository_path`
- `current_branch`
- `expected_branch`
- `branch_matches_expected`
- `head_sha`
- `expected_base_sha`
- `head_matches_expected`
- `working_tree_clean_before`
- `working_tree_clean_after`
- `no_target_repo_modifications`
- `execution_mode`
- `plan_read`
- `parent_run_verified`
- `target_repository_verified`
- `acceptance_criteria_met`
- `blocked`

Validate `evidence.json` with `python3 -m json.tool` before returning.

# Forbidden Operations

Do not:

- modify any file in `bot_trading`;
- commit or push in `bot_trading`;
- deploy;
- change target branches;
- run reset, clean or destructive Git operations;
- modify credentials or secrets;
- use `opencode --auto`;
- create P000006;
- write a Planner review or GO/NO_GO decision;
- manually alter workflow lifecycle state;
- invoke `complete-run` or `publish-run` from inside OpenCode.

# Acceptance Criteria

- [ ] P000005 and parent R000004 are correctly identified.
- [ ] Target repository is verified at the required path.
- [ ] Target branch is `feature/GRU`.
- [ ] Target HEAD equals `1afb326bead32e2f928530e33850b5651e46b9b1`.
- [ ] Target working tree is clean before and after.
- [ ] No target-repository files are modified.
- [ ] Required R000005 evidence is physically created and valid.
- [ ] OpenCode returns control without performing lifecycle transitions or Planner decisions.
- [ ] The local orchestrator, without human recovery, transitions the run to `REVIEW_READY` and publishes R000005 evidence.

# Final Proof Rule

This PLAN is GO only if the full run reaches durable `REVIEW_READY` and its evidence is published to `Trochez/ai-control-plane/main` without any manual local repair, manual re-run of `run-opencode-session`, manual evidence creation, or manual invocation of `complete-run`/`publish-run`.

If the detached worker fails, evidence remains incomplete after its bounded attempts, or human recovery is required, the final unattended proof is NO_GO.

# Completion Contract

After creating and validating the required run evidence, return normally. The remediated local orchestrator must own evidence validation, retry if required, lifecycle completion, and publication. ChatGPT owns the final review.
